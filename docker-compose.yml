services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: wesplit
      POSTGRES_PASSWORD: wesplit_local
      POSTGRES_DB: wesplit_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
